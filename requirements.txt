# ====================================================================
# 프로덕션 의존성 (현재 검증된 버전으로 고정)
# - 재현 가능한 빌드를 위해 == 로 정확한 버전 핀
# - 업그레이드 시에는 로컬에서 통합 테스트 후 이 파일을 함께 수정할 것
# ====================================================================

# Windows 시스템 API (이벤트 로그, 프로세스 제어 등)
pywin32==308

# 시스템/프로세스 정보 수집
psutil==7.1.3

# 웹 프레임워크 + ASGI 서버
fastapi==0.135.1
uvicorn==0.41.0

# 데이터 검증/직렬화 (FastAPI가 내부적으로 의존)
pydantic==2.10.3

# HTTP 클라이언트 (에이전트가 백엔드로 리포트 전송)
requests==2.32.3

# 로컬 LLM (Gemma) 호출용 Ollama 공식 클라이언트
ollama==0.6.2

# .env 파일 로드
python-dotenv==1.1.0

# 파일 시스템 변경 감시 (랜섬웨어 허니팟용)
watchdog==4.0.2
