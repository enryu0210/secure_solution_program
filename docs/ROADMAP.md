# 로드맵 — 남은 개선 작업

마지막 업데이트: **2026-05-08** (커밋 `e495883` 시점)

---

## ✅ 최근 완료된 작업

| 커밋 | 내용 |
|---|---|
| `c5d344d` | Groq → Ollama 로컬 LLM(Gemma) 교체 |
| `371445b` | 기본 LLM 모델 gemma3 → gemma4 승격 |
| `39d2e42` | AI 보안 분석 프롬프트 개선 (응답 양식 이탈 문제 해결) |
| `e495883` | 의존성 버전 고정 + 표준 로깅(`logger.py`) + 헬스체크(`/api/v1/health`) |
| 별도 | `.env` 모델을 `gemma4:latest`(8B)로 업그레이드 (사용자 직접) |

---

## 🟡 단기 작업 (1~2시간)

### 2-2. 시스템 프롬프트 외부 파일 분리
- **현재**: `backend/main.py` 내부 `SECURITY_ANALYSIS_SYSTEM_PROMPT` 모듈 상수
- **개선**: `backend/prompts/security_analysis.md` 등 텍스트 파일로 분리
- **이유**: 프롬프트 수정 시 코드 재배포 불필요, 버전 관리/A-B 테스트 용이

### 2-3. 프로젝트 전용 `CLAUDE.md` 작성
- **현재**: 글로벌 `~/.claude/CLAUDE.md`만 존재
- **추가할 내용**: 보안 솔루션 도메인 규칙, Ollama 운영 가이드, 백엔드/에이전트 분리 규칙

---

## 🟠 중기 작업 (반나절 단위)

### 3-1. AI 분석 결과 구조화
- **현재**: 마크다운 텍스트만 SQLite에 저장
- **개선**: '안전 / 주의 / 심각' 평가를 별도 컬럼으로 추출
- **효과**: 대시보드에서 "심각 상태 에이전트만 필터링" 같은 기능 가능

### 3-2. 테스트 보강
- **현재**: `test_ransomware.py` 1개뿐
- **추가**: 스캐너 6종 단위 테스트, 엔드포인트 7개 통합 테스트, AI 분석 mock 테스트

### 3-3. 에이전트 인증 토큰
- **현재 위험**: `machine_id`만으로 누구나 데이터 푸쉬 가능 (위변조 위험)
- **개선**: API 키 또는 HMAC 서명 기반 인증

### 3-4. CORS 도메인 제한
- **현재**: `backend/main.py`의 `allow_origins=["*"]`
- **개선**: 운영 도메인 화이트리스트 적용 (운영 시 필수)

---

## 🔴 장기 작업 (1일+)

| 번호 | 작업 | 비고 |
|---|---|---|
| **4-1** | Docker 컨테이너화 | docker-compose로 Ollama + 백엔드 묶기 |
| **4-2** | 알림 기능 | 심각 상태 시 이메일/슬랙/디스코드 (보안 솔루션의 USP) |
| **4-3** | 프론트엔드 모듈화 | 단일 `frontend/index.html` → React/Vue 등 |
| **4-4** | 에이전트 그룹/태깅 | B2B 부서별/지점별 분류 |

---

## 추천 진행 순서

| 우선 가치 | 권장 순서 |
|---|---|
| **빠른 정리감** | 2-2 → 2-3 |
| **운영 안정성** | 3-3 (인증) → 3-4 (CORS) → 4-1 (Docker) |
| **제품 가치** | 4-2 (알림) → 3-1 (구조화) |
